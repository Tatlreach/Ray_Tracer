
	inline vec3 operator*(float t, const vec3& v);
is too many params if in the .h
but works in the .cpp


///linear interpolation //clearer
start + t*(end-start)


make a vec from cam to sphere center, sVec
project sVec onto unit ray.dirction, dot(sVec,ray.direction)
scale ray.direction by this dot product, edgeVec
edgeVec-sVec gives vector from sphere center outward, radiusVec
if( radiusVec.length() < R^3 ) it's a collision